jest.setTimeout(30 * 1000);

describe('Neo4j Knowledge Graph', () => {

    test('TB08 - Add references to Neo4j database exists', async () => {
    });

    test('TB09 - Add references to Neo4j database non existing', async () => {
    });

    test('TB10 - Get references from Neo4j depth 1', async () => {
    });

    test('TB11 - Get references from Neo4j of variable depth', async () => {
    });
});